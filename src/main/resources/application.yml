spring:
  application:
    name: despesas
  #Mysql Local
#  datasource:
#    username: root
#    password: Suro3624#
#    url: jdbc:mysql://0.0.0.0/despesas?createDatabaseIfNotExist=true
#    driver-class-name: com.mysql.cj.jdbc.Driver
  #Postgres Heroku
  datasource:
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    url: jdbc:postgresql://${DB_HOSTNAME}:${DB_PORT}/${DB_NAME}?createDatabaseIfNotExist=true
    initialization-mode: always
    continue-on-error: true
  jpa:
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.MySQL8Dialect
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  #security:
  #  user:
  #    name: lsena
  #    password: mudar@12
  #    role: ADMIN

com:
  zaxxer:
    hikari:
      HikariDataSource:
        driverclassname: org.postgresql.Driver

jwt:
  config:
    login-url: /auth/login
    type: encrypted